# BajaDentalGuide Agent Notes

## Run the app

From repo root:

```bash
cd web
npm run dev
```

## Build the app

From repo root:

```bash
cd web
npm run build
```

run npm run build before committing
